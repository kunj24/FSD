<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
  <div class="container">
    <header>
      <h1><%= title %></h1>
      <p class="subtitle">Enter your income from two sources to calculate your total income</p>
    </header>

    <main>
      <form action="/calculate" method="POST" class="tax-form">
        <div class="form-group">
          <label for="income1">Income Source 1 (e.g., Salary, Wages):</label>
          <div class="input-wrapper">
            <span class="currency-symbol">₹</span>
            <input 
              type="text" 
              id="income1" 
              name="income1" 
              placeholder="0.00"
              value="<%= formData ? formData.income1 || '' : '' %>"
              class="<%= errors && errors.income1 ? 'error' : '' %>"
              required
            >
          </div>
          <% if (errors && errors.income1) { %>
            <div class="error-message"><%= errors.income1 %></div>
          <% } %>
        </div>

        <div class="form-group">
          <label for="income2">Income Source 2 (e.g., Freelance, Business):</label>
          <div class="input-wrapper">
            <span class="currency-symbol">₹</span>
            <input 
              type="text" 
              id="income2" 
              name="income2" 
              placeholder="0.00"
              value="<%= formData ? formData.income2 || '' : '' %>"
              class="<%= errors && errors.income2 ? 'error' : '' %>"
              required
            >
          </div>
          <% if (errors && errors.income2) { %>
            <div class="error-message"><%= errors.income2 %></div>
          <% } %>
        </div>

        <button type="submit" class="calculate-btn">Calculate Total Income</button>
      </form>

        <div class="info-section">
        <h3>Important Notes:</h3>
        <ul>
          <li>Enter amounts in INR (rupee signs and commas are optional)</li>
          <li>All calculations are performed securely on the server</li>
          <li>Both income sources must be non-negative values</li>
          <li>Maximum income per source: ₹999,999,999</li>
        </ul>
      </div>
    </main>

    <footer>
      <p>&copy; 2025 Tax Income Calculator - For Educational Purposes</p>
    </footer>
  </div>

  <!-- No client-side calculation scripts -->
</body>
</html>