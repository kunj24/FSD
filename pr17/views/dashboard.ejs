<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EduManager - Student Management System</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            /* Primary Colors */
            --primary-50: #f0f9ff;
            --primary-100: #e0f2fe;
            --primary-200: #bae6fd;
            --primary-300: #7dd3fc;
            --primary-400: #38bdf8;
            --primary-500: #0ea5e9;
            --primary-600: #0284c7;
            --primary-700: #0369a1;
            --primary-800: #075985;
            --primary-900: #0c4a6e;
            
            /* Secondary Colors */
            --secondary-50: #fdf4ff;
            --secondary-100: #fae8ff;
            --secondary-200: #f5d0fe;
            --secondary-300: #f0abfc;
            --secondary-400: #e879f9;
            --secondary-500: #d946ef;
            --secondary-600: #c026d3;
            --secondary-700: #a21caf;
            --secondary-800: #86198f;
            --secondary-900: #701a75;
            
            /* Neutral Colors */
            --gray-50: #f9fafb;
            --gray-100: #f3f4f6;
            --gray-200: #e5e7eb;
            --gray-300: #d1d5db;
            --gray-400: #9ca3af;
            --gray-500: #6b7280;
            --gray-600: #4b5563;
            --gray-700: #374151;
            --gray-800: #1f2937;
            --gray-900: #111827;
            
            /* Success, Warning, Error */
            --success-500: #10b981;
            --warning-500: #f59e0b;
            --error-500: #ef4444;
            
            /* Background */
            --bg-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            --bg-light: #ffffff;
            --bg-surface: #f8fafc;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Poppins', -apple-system, BlinkMacSystemFont, sans-serif;
            background: var(--bg-gradient);
            min-height: 100vh;
            color: var(--gray-800);
            line-height: 1.6;
            font-size: 16px;
        }
        
        .main-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 24px;
        }
        
        /* Header Section */
        .header-section {
            background: rgba(255, 255, 255, 0.98);
            backdrop-filter: blur(20px);
            border-radius: 24px;
            padding: 48px 40px;
            margin-bottom: 32px;
            text-align: center;
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
            border: 1px solid rgba(255, 255, 255, 0.3);
            position: relative;
            overflow: hidden;
        }
        
        .header-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: var(--bg-gradient);
        }
        
        .header-section h1 {
            font-size: 3.5rem;
            font-weight: 800;
            background: var(--bg-gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 12px;
            letter-spacing: -0.02em;
        }
        
        .header-section p {
            font-size: 1.25rem;
            color: var(--gray-600);
            font-weight: 400;
            max-width: 600px;
            margin: 0 auto;
        }
        
        /* Dashboard Layout */
        .dashboard-layout {
            display: grid;
            grid-template-columns: 320px 1fr;
            gap: 32px;
            align-items: start;
        }
        
        /* Sidebar Styling */
        .sidebar {
            background: rgba(255, 255, 255, 0.98);
            backdrop-filter: blur(20px);
            border-radius: 24px;
            padding: 32px;
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
            border: 1px solid rgba(255, 255, 255, 0.3);
            position: sticky;
            top: 24px;
        }
        
        .sidebar-header {
            margin-bottom: 32px;
        }
        
        .sidebar-title {
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--gray-800);
            margin-bottom: 8px;
            display: flex;
            align-items: center;
            gap: 12px;
        }
        
        .sidebar-subtitle {
            color: var(--gray-600);
            font-size: 0.875rem;
        }
        
        .stats-container {
            display: flex;
            flex-direction: column;
            gap: 16px;
            margin-bottom: 32px;
        }
        
        .stat-card {
            background: var(--bg-gradient);
            color: white;
            padding: 24px;
            border-radius: 16px;
            position: relative;
            overflow: hidden;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.1);
        }
        
        .stat-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(255, 255, 255, 0.1);
            opacity: 0;
            transition: opacity 0.3s ease;
        }
        
        .stat-card:hover::before {
            opacity: 1;
        }
        
        .stat-content {
            position: relative;
            z-index: 1;
            text-align: center;
        }
        
        .stat-number {
            font-size: 2.75rem;
            font-weight: 800;
            margin-bottom: 8px;
            display: block;
        }
        
        .stat-label {
            font-size: 0.875rem;
            font-weight: 500;
            opacity: 0.95;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
        }
        
        /* Action Button */
        .add-student-btn {
            background: var(--primary-600);
            color: white;
            padding: 16px 24px;
            border-radius: 16px;
            text-decoration: none;
            font-weight: 600;
            font-size: 1rem;
            display: flex;
            align-items: center;
            gap: 12px;
            transition: all 0.3s ease;
            box-shadow: 0 4px 12px rgba(2, 132, 199, 0.3);
            border: none;
            cursor: pointer;
            width: 100%;
            justify-content: center;
        }
        
        .add-student-btn:hover {
            background: var(--primary-700);
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(2, 132, 199, 0.4);
        }
        
        /* Main Content Area */
        .main-content {
            background: rgba(255, 255, 255, 0.98);
            backdrop-filter: blur(20px);
            border-radius: 24px;
            padding: 32px;
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
            border: 1px solid rgba(255, 255, 255, 0.3);
        }
        
        .content-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 32px;
            flex-wrap: wrap;
            gap: 24px;
        }
        
        .content-title {
            font-size: 2rem;
            font-weight: 700;
            color: var(--gray-800);
            display: flex;
            align-items: center;
            gap: 12px;
        }
        
        .btn {
            padding: 15px 30px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            text-decoration: none;
            border-radius: 12px;
            border: none;
            cursor: pointer;
            font-size: 16px;
            font-weight: 600;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            gap: 8px;
            box-shadow: 0 8px 25px rgba(102, 126, 234, 0.3);
        }
        
        .btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 12px 35px rgba(102, 126, 234, 0.4);
        }
        
        .btn-danger {
            background: linear-gradient(135deg, #ff6b6b 0%, #ee5a52 100%);
            box-shadow: 0 8px 25px rgba(255, 107, 107, 0.3);
        }
        
        .btn-danger:hover {
            box-shadow: 0 12px 35px rgba(255, 107, 107, 0.4);
        }
        
        .btn-secondary {
            background: linear-gradient(135deg, #6c757d 0%, #5a6268 100%);
            box-shadow: 0 8px 25px rgba(108, 117, 125, 0.3);
        }
        
        /* Search Functionality */
        .search-container {
            position: relative;
            flex: 1;
            max-width: 400px;
        }
        
        .search-input {
            width: 100%;
            padding: 16px 20px 16px 48px;
            border: 2px solid var(--gray-200);
            border-radius: 16px;
            font-size: 1rem;
            background: white;
            transition: all 0.3s ease;
            font-family: inherit;
        }
        
        .search-input:focus {
            outline: none;
            border-color: var(--primary-500);
            box-shadow: 0 0 0 3px rgba(14, 165, 233, 0.1);
        }
        
        .search-input::placeholder {
            color: var(--gray-400);
        }
        
        .search-icon {
            position: absolute;
            left: 16px;
            top: 50%;
            transform: translateY(-50%);
            color: var(--gray-400);
            font-size: 1.125rem;
        }
        /* Button Styles */
        .btn {
            padding: 12px 20px;
            border: none;
            border-radius: 12px;
            font-size: 0.875rem;
            font-weight: 600;
            cursor: pointer;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 8px;
            transition: all 0.3s ease;
            font-family: inherit;
        }
        
        .btn-primary {
            background: var(--primary-600);
            color: white;
            box-shadow: 0 4px 12px rgba(2, 132, 199, 0.3);
        }
        
        .btn-primary:hover {
            background: var(--primary-700);
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(2, 132, 199, 0.4);
        }
        
        .btn-secondary {
            background: var(--gray-100);
            color: var(--gray-700);
            border: 1px solid var(--gray-200);
        }
        
        .btn-secondary:hover {
            background: var(--gray-200);
            transform: translateY(-1px);
        }
        
        .btn-danger {
            background: var(--error-500);
            color: white;
            box-shadow: 0 4px 12px rgba(239, 68, 68, 0.3);
        }
        
        .btn-danger:hover {
            background: #dc2626;
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(239, 68, 68, 0.4);
        }
        
        .btn-sm {
            padding: 8px 16px;
            font-size: 0.75rem;
        }
        
        /* Message Alerts */
        .message {
            padding: 16px 20px;
            border-radius: 16px;
            margin-bottom: 24px;
            font-weight: 500;
            text-align: center;
            font-weight: 500;
            backdrop-filter: blur(10px);
        }
        
        .success {
            background: rgba(16, 185, 129, 0.1);
            color: var(--success-500);
            border: 2px solid rgba(16, 185, 129, 0.2);
            display: flex;
            align-items: center;
            gap: 12px;
        }
        
        .error {
            background: rgba(239, 68, 68, 0.1);
            color: var(--error-500);
            border: 2px solid rgba(239, 68, 68, 0.2);
            display: flex;
            align-items: center;
            gap: 12px;
        }
        
        /* Table Container */
        .students-table-container {
            background: white;
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 20px 40px -12px rgba(0, 0, 0, 0.25);
            border: 1px solid var(--gray-200);
        }
        
        /* Table Styling */
        table {
            width: 100%;
            border-collapse: collapse;
        }
        
        th, td {
            padding: 20px 24px;
            text-align: left;
            border-bottom: 1px solid var(--gray-100);
        }
        
        th {
            background: var(--gray-50);
            font-weight: 700;
            color: var(--gray-700);
            font-size: 0.875rem;
            text-transform: uppercase;
            letter-spacing: 0.05em;
        }
        
        tbody tr {
            transition: background-color 0.2s ease;
        }
        
        tbody tr:hover {
            background: var(--gray-50);
        }
        
        tbody tr:last-child td {
            border-bottom: none;
        }
        
        /* Status Badge */
        .status-badge {
            padding: 8px 16px;
            border-radius: 20px;
            font-size: 0.75rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            display: inline-flex;
            align-items: center;
            gap: 6px;
        }
        
        .paid {
            background: rgba(16, 185, 129, 0.1);
            color: var(--success-500);
        }
        
        .unpaid {
            background: rgba(239, 68, 68, 0.1);
            color: var(--error-500);
        }
        
        /* Action Buttons */
        .action-buttons {
            display: flex;
            gap: 8px;
            align-items: center;
        }
        
        /* Empty State */
        .empty-state {
            text-align: center;
            padding: 80px 40px;
            color: var(--gray-500);
        }
        
        .empty-state i {
            font-size: 4rem;
            margin-bottom: 24px;
            opacity: 0.5;
            color: var(--gray-400);
        }
        
        .empty-state h3 {
            margin-bottom: 12px;
            font-size: 1.5rem;
            color: var(--gray-700);
            font-weight: 600;
        }
        
        .empty-state p {
            margin-bottom: 32px;
            font-size: 1.125rem;
        }
        
        /* Responsive Design */
        @media (max-width: 1024px) {
            .dashboard-layout {
                grid-template-columns: 280px 1fr;
                gap: 24px;
            }
            
            .header-section h1 {
                font-size: 2.5rem;
            }
        }
        
        @media (max-width: 768px) {
            .main-container {
                padding: 16px;
            }
            
            .dashboard-layout {
                grid-template-columns: 1fr;
                gap: 24px;
            }
            
            .sidebar {
                position: static;
                order: 2;
            }
            
            .main-content {
                order: 1;
            }
            
            .header-section {
                padding: 32px 24px;
            }
            
            .header-section h1 {
                font-size: 2rem;
            }
            
            .content-header {
                flex-direction: column;
                align-items: stretch;
                gap: 16px;
            }
            
            .search-container {
                max-width: 100%;
            }
            
            table {
                font-size: 0.875rem;
            }
            
            th, td {
                padding: 16px 12px;
            }
            
            .action-buttons {
                flex-direction: column;
                gap: 8px;
            }
        }
        
        @media (max-width: 480px) {
            .header-section h1 {
                font-size: 1.75rem;
            }
            
            th, td {
                padding: 12px 8px;
            }
        }
        
        tr:hover {
            background: rgba(102, 126, 234, 0.05);
            transition: all 0.3s ease;
        }
        
        .status-badge {
            padding: 8px 16px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        
        .paid {
            background: linear-gradient(135deg, #28a745 0%, #20c997 100%);
            color: white;
        }
        
        .unpaid {
            background: linear-gradient(135deg, #dc3545 0%, #fd7e14 100%);
            color: white;
        }
        
        .action-buttons {
            display: flex;
            gap: 10px;
            flex-wrap: wrap;
        }
        
        .btn-sm {
            padding: 8px 16px;
            font-size: 13px;
            border-radius: 8px;
        }
        
        .empty-state {
            text-align: center;
            padding: 60px 20px;
            color: #6c757d;
        }
        
        .empty-state i {
            font-size: 4rem;
            margin-bottom: 20px;
            opacity: 0.5;
        }
        
        .empty-state h3 {
            margin-bottom: 10px;
            font-size: 1.5rem;
        }
        
        @media (max-width: 768px) {
            .dashboard-grid {
                grid-template-columns: 1fr;
            }
            
            .content-header {
                flex-direction: column;
                align-items: stretch;
            }
            
            .search-container {
                max-width: 100%;
            }
            
            table {
                font-size: 14px;
            }
            
            th, td {
                padding: 12px 8px;
            }
            
            .action-buttons {
                flex-direction: column;
            }
        }
            color: #666;
            margin-top: 5px;
        }
        
        .no-students {
            text-align: center;
            padding: 50px 20px;
            color: #666;
        }
        
        .no-students h3 {
            margin-bottom: 10px;
            color: #333;
        }
        
        .no-students p {
            margin-bottom: 20px;
        }
        
        .no-students .btn {
            display: inline-block;
            padding: 15px 25px;
            font-size: 16px;
            margin-top: 15px;
        }
        
        @media (max-width: 768px) {
            .container {
                padding: 10px;
            }
            
            header h1 {
                font-size: 2rem;
            }
            
            .actions {
                flex-direction: column;
                align-items: stretch;
                gap: 15px;
            }
            
            .actions h2 {
                text-align: center;
                order: 1;
            }
            
            .btn {
                text-align: center;
                order: 2;
                font-size: 16px;
                padding: 15px;
            }
            
            table {
                font-size: 14px;
            }
            
            th, td {
                padding: 10px 8px;
            }
            
            .action-buttons {
                flex-direction: column;
            }
            
            .stats {
                grid-template-columns: repeat(2, 1fr);
            }
    </style>
</head>
<body>
    <div class="main-container">
        <div class="header-section">
            <h1><i class="fas fa-graduation-cap"></i> EduManager</h1>
            <p>Professional Student Management System</p>
        </div>
        
        <% if (message) { %>
            <div class="message success">
                <i class="fas fa-check-circle"></i> <%= message %>
            </div>
        <% } %>
        
        <% if (error) { %>
            <div class="message error">
                <i class="fas fa-exclamation-triangle"></i> <%= error %>
            </div>
        <% } %>
        
        <div class="dashboard-layout">
            <div class="sidebar">
                <div class="sidebar-header">
                    <h3 class="sidebar-title">
                        <i class="fas fa-chart-pie"></i> Overview
                    </h3>
                    <p class="sidebar-subtitle">Quick statistics and actions</p>
                </div>
                
                <div class="stats-container">
                    <div class="stat-card">
                        <div class="stat-content">
                            <div class="stat-number"><%= students.length %></div>
                            <div class="stat-label">
                                <i class="fas fa-users"></i> Total Students
                            </div>
                        </div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-content">
                            <div class="stat-number"><%= students.filter(s => s.feesPaid).length %></div>
                            <div class="stat-label">
                                <i class="fas fa-check-circle"></i> Fees Paid
                            </div>
                        </div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-content">
                            <div class="stat-number"><%= students.filter(s => !s.feesPaid).length %></div>
                            <div class="stat-label">
                                <i class="fas fa-clock"></i> Pending Fees
                            </div>
                        </div>
                    </div>
                </div>
                
                <a href="/add" class="add-student-btn">
                    <i class="fas fa-plus"></i> Add New Student
                </a>
            </div>
            
            <div class="main-content">
                <div class="content-header">
                    <h2 class="content-title">
                        <i class="fas fa-users"></i> Student Directory
                    </h2>
                    <div class="search-container">
                        <i class="fas fa-search search-icon"></i>
                        <input type="text" class="search-input" placeholder="Search students by name, email, or subject..." id="searchInput">
                    </div>
                </div>
                
                <div class="students-table-container">
                    <% if (students.length > 0) { %>
                        <table id="studentsTable">
                            <thead>
                                <tr>
                                    <th><i class="fas fa-user"></i> Name</th>
                                    <th><i class="fas fa-envelope"></i> Email</th>
                                    <th><i class="fas fa-phone"></i> Phone</th>
                                    <th><i class="fas fa-layer-group"></i> Grade</th>
                                    <th><i class="fas fa-book"></i> Subject</th>
                                    <th><i class="fas fa-credit-card"></i> Status</th>
                                    <th><i class="fas fa-cogs"></i> Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% students.forEach(student => { %>
                                    <tr class="student-row">
                                        <td class="searchable"><strong><%= student.name %></strong></td>
                                        <td class="searchable"><%= student.email %></td>
                                        <td><%= student.phone %></td>
                                        <td><%= student.grade %></td>
                                        <td class="searchable"><%= student.subject %></td>
                                        <td>
                                            <span class="status-badge <%= student.feesPaid ? 'paid' : 'unpaid' %>">
                                                <i class="fas fa-<%= student.feesPaid ? 'check' : 'clock' %>"></i>
                                                <%= student.feesPaid ? 'Paid' : 'Pending' %>
                                            </span>
                                        </td>
                                        <td>
                                            <div class="action-buttons">
                                                <a href="/edit/<%= student._id %>" class="btn btn-secondary btn-sm">
                                                    <i class="fas fa-edit"></i> Edit
                                                </a>
                                                <form method="POST" action="/students/<%= student._id %>?_method=DELETE" style="display: inline;">
                                                    <button type="submit" class="btn btn-danger btn-sm" 
                                                            onclick="return confirm('Are you sure you want to delete this student?')">
                                                        <i class="fas fa-trash"></i> Delete
                                                    </button>
                                                </form>
                                            </div>
                                        </td>
                                    </tr>
                                <% }) %>
                            </tbody>
                        </table>
                    <% } else { %>
                        <div class="empty-state">
                            <i class="fas fa-user-graduate"></i>
                            <h3>No students found</h3>
                            <p>Start building your student database by adding your first student!</p>
                            <a href="/add" class="btn">
                                <i class="fas fa-plus"></i> Add First Student
                            </a>
                        </div>
                    <% } %>
                </div>
            </div>
        </div>
    </div>
    
    <script>
        // Search functionality
        document.getElementById('searchInput').addEventListener('input', function() {
            const searchTerm = this.value.toLowerCase();
            const rows = document.querySelectorAll('.student-row');
            
            rows.forEach(row => {
                const searchableContent = Array.from(row.querySelectorAll('.searchable'))
                    .map(cell => cell.textContent.toLowerCase())
                    .join(' ');
                
                if (searchableContent.includes(searchTerm)) {
                    row.style.display = '';
                } else {
                    row.style.display = 'none';
                }
            });
        });
    </script>
</body>
</html>